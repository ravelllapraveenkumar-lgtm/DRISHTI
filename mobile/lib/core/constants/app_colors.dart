import 'package:flutter/material.dart';

// =====================================================================
// DRISHTI Mobile App: High-Contrast Field Inspection Palette
// Clean government theme optimized for outdoor/sunlight legibility
// =====================================================================

class AppColors {
  // Brand Primary & Secondary
  static const Color primaryNavy = Color(0xFF0F2744);
  static const Color primaryDark = Color(0xFF09182A);
  static const Color saffronAccent = Color(0xFFD97706);
  static const Color saffronLight = Color(0xFFFEF3C7);

  // Status & Verdict Colors
  static const Color statusSuccess = Color(0xFF059669);
  static const Color statusSuccessBg = Color(0xFFECFDF5);
  static const Color statusWarning = Color(0xFFD97706);
  static const Color statusWarningBg = Color(0xFFFFFBEB);
  static const Color statusCritical = Color(0xFFDC2626);
  static const Color statusCriticalBg = Color(0xFFFEF2F2);
  static const Color statusInfo = Color(0xFF2563EB);
  static const Color statusInfoBg = Color(0xFFEFF6FF);

  // Neutral Scales (WCAG AA compliant)
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF1F5F9);
  static const Color border = Color(0xFFE2E8F0);
  static const Color borderDark = Color(0xFFCBD5E1);

  // Typography
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF475569);
  static const Color textMuted = Color(0xFF94A3B8);
  static const Color textWhite = Color(0xFFFFFFFF);

  // Sync Status Colors
  static const Color syncPending = Color(0xFF6366F1);
  static const Color syncSyncing = Color(0xFF0284C7);
  static const Color syncSynced = Color(0xFF10B981);
  static const Color syncFailed = Color(0xFFEF4444);
}
